16:28 2008-4-15
1.net.java.sip.communicator.impl.protocol.sip.AddressResolverImpl的public Hop resolveAddress(Hop inputAddress)
修改了其中的业务逻辑，修改后逻辑如下：
	a 判断“输入的地址的Host值是否为一个合法IP地址”AND“输入地址的port是否为一个合法端口号”
	b 如果a为真，则直接将输入地址返回
	c 否则，按照现有逻辑处理。

2.net.java.sip.communicator.util.NetworkUtils增加了两个静态方法
public static boolean isValidIPv4Addr(String ipStr)
public static boolean isValidPort(int port)



